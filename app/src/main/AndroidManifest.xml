<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ankitkumar.expensecalculator">

    <application android:theme="@style/AppTheme" android:supportsRtl="true" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true">
        -<activity android:name=".MainActivity">


        -<intent-filter>

        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>

    </intent-filter>

    </activity>
        <activity android:label="Expenses" android:name=".Main2Activity"/>

        <activity android:label="Income" android:name=".IncomePage"/>

        <activity android:name=".GraphActivity"/>
    </application>

</manifest>